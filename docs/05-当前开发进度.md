# 当前开发进度记录

## 已完成阶段

### 阶段 1：工程初始化
完成时间：2026-03-21

已完成：
- 解决方案与项目骨架创建
- 独立前端框架骨架创建
- 基础目录初始化
- 项目引用关系建立

### 阶段 2：基础依赖配置
完成时间：2026-03-21

已完成：
- 中央包版本管理配置
- LibreHardwareMonitor / TraceEvent / SQLite / Serilog 依赖接入
- NuGet restore 验证
- 后端整体 build 验证

### 阶段 3：统一基础能力
完成时间：2026-03-21

已完成：
- 配置读取与校验
- 配置提供器
- Serilog 日志接入
- 全局异常处理中间件补强

### 阶段 4：硬件采集 MVP（任务 4~6）
完成时间：2026-03-21

已完成：
- LibreHardwareMonitor 封装
- 一次性硬件采样接口
- 标准化硬件快照输出
- 硬件定时采样
- 内存缓存与批量持久化调用链

### 阶段 5.1：网络采集 MVP - ETW 接入（任务 7）
完成时间：2026-03-21

已完成：
- ETW 网络采集器基础实现
- Kernel NetworkTCPIP provider 启动链路
- TCP/UDP 收发事件订阅
- PID、方向、字节数、本地/远端地址与端口解析
- NetworkTraceEvent 标准事件模型
- 提权冒烟验证通过（Collector running=True，成功捕获真实网络事件）

### 阶段 5.2：网络采集 MVP - 进程解析与 app registry（任务 8）
完成时间：2026-03-21

已完成：
- PID -> process name 解析
- PID -> executable path 解析
- display name 提取
- app key 生成（基于规范化路径 / 进程名的 SHA256）
- 进程解析缓存
- 内存版 app registry 服务

### 阶段 5.3：网络采集 MVP - WAN/LAN 分类（任务 9）
完成时间：2026-03-21

已完成：
- AddressClassifier 抽象接口与实现补齐
- Loopback 识别
- 私网 IPv4 / IPv6 ULA / Link-Local 识别
- 本机活动网卡子网识别（30 秒缓存刷新）
- 广播 / 组播 / 未指定地址归类为 Other
- 默认规则配置化（私网、本地子网、Loopback 开关 + 额外 LAN/WAN CIDR）
- appsettings 默认配置补齐
- 冒烟验证通过（分类结果与配置覆盖规则生效）

### 阶段 5.4：网络采集 MVP - 网络聚合器（任务 10）
完成时间：2026-03-21

已完成：
- TrafficAggregator 实现并接入 ETW 事件流
- 按实时窗口计算总上传/下载速率
- 按 app 维度聚合实时上传/下载速率
- 按 WAN/LAN 维度聚合实时速率
- 按配置粒度生成时间桶聚合结果（TrafficBucket）
- 完成桶转入待持久化队列
- AggregationHostedService 接入网络桶持久化调用链
- 冒烟验证通过（实时速率、Top Apps、WAN/LAN、时间桶输出正常）

### 阶段 6.1：数据存储 MVP - SQLite 数据库与建表（任务 11）
完成时间：2026-03-21

已完成：
- SQLite 连接工厂实现
- 数据库初始化器实现
- 启动时自动建库建表
- `settings` 表创建
- `hardware_samples` 表与时间索引创建
- `app_registry` 表创建
- `network_usage_agg` 表与相关索引创建
- 默认 `settings(id=1)` 初始化写入
- 冒烟验证通过（数据库文件、表、索引、默认配置行均已生成）

### 阶段 6.2：数据存储 MVP - 仓储层实现（任务 12）
完成时间：2026-03-21

已完成：
- HardwareRepository 真实 SQLite 批量插入实现
- HardwareRepository 时间区间查询实现
- NetworkTrafficRepository 真实 SQLite 批量插入实现
- app_registry Upsert 实现
- network_usage_agg 按桶落库实现
- app 维度区间汇总查询实现
- 原始桶区间查询实现
- 冒烟验证通过（硬件样本、网络桶、app 汇总均可正确写入和查询）

### 阶段 6.3：数据存储 MVP - 保留策略（任务 13）
完成时间：2026-03-21

已完成：
- RetentionService 真实清理逻辑实现
- 按 `HistoryRetentionDays` 清理过期 `hardware_samples`
- 按 `HistoryRetentionDays` 清理过期 `network_usage_agg`
- 清理无引用且已过期的 `app_registry`
- 清理后执行 WAL checkpoint 与 VACUUM 回收空间
- CleanupHostedService 启动即执行一次清理，并保留周期性清理
- 冒烟验证通过（旧硬件样本、旧网络桶、旧孤儿 app 记录均被清理，近期数据保留）

### 阶段 7.1：后端服务 MVP - Web API（任务 14）
完成时间：2026-03-21

已完成：
- `/health` 健康检查可用
- `GET /api/overview` 真实返回总览数据
- `GET /api/hardware/realtime` 真实返回硬件实时数据
- `GET /api/hardware/history` 接入硬件历史查询（支持时间区间，默认最近 1 小时）
- `GET /api/network/realtime` 真实返回网络实时速率
- `GET /api/network/apps` 接入 app 流量汇总查询（支持时间区间与 topN，默认最近 1 小时）
- `GET /api/settings` 从 SQLite / 当前配置读取设置
- `POST /api/settings` 支持参数校验并持久化到 SQLite
- 冒烟验证通过（服务启动后接口可访问并返回预期结构）

### 阶段 7.2：后端服务 MVP - SignalR（任务 15）
完成时间：2026-03-21

已完成：
- `MonitorHub` 实时推送事件名约定补齐
- 实现 `MonitorRealtimeBroadcaster`
- 推送硬件实时数据：`hardwareRealtime`
- 推送网络实时数据：`networkRealtime`
- 推送 Top App 实时排行：`topAppsRealtime`
- 实现 SignalR 后台推送 HostedService
- Web API 启动时自动接入推送链路
- 冒烟验证通过（一次广播可成功发出 3 条 Hub 消息）

### 阶段 7.3：后端服务 MVP - 本地端口配置化（任务 16）
完成时间：2026-03-21

已完成：
- 实现启动前从 SQLite `settings` 表加载持久化配置
- 持久化配置可覆盖 `Monitor` 节点中的端口与采样参数
- 服务启动时按数据库中保存的 HTTP 端口监听
- `POST /api/settings` 已支持后续页面修改并写入 SQLite
- 冒烟验证通过（将数据库中的端口改为 58091 后，服务可直接在该端口启动）

### 阶段 8.1：前端页面 MVP - 基础页面（任务 17）
完成时间：2026-03-21

已完成：
- 前端整体布局样式补强（侧边栏 + 内容区）
- 总览页基础框架搭建
- 网络页基础框架搭建
- 设置页基础框架搭建
- 前端 API 访问封装补齐
- 基础通用组件补齐（PageHeader / MetricCard）
- 总览页接入 `/api/overview`
- 网络页接入 `/api/network/apps`
- 设置页接入 `/api/settings` 与 `POST /api/settings`
- 前端生产构建验证通过
- 修复前端配置文件 BOM 导致的 Vite 构建问题

### 阶段 8.2：前端页面 MVP - 总览页（任务 18）
完成时间：2026-03-21

已完成：
- 总览页核心指标展示补齐
- 展示 CPU 使用率
- 展示内存使用率
- 展示 CPU 温度
- 展示磁盘温度
- 展示 CPU 频率
- 展示开机时长
- 展示当前总上传 / 下载速率
- 补充 WAN / LAN 实时速率展示
- 补充最后更新时间与运行状态展示
- 接入自动轮询刷新（2 秒）
- 前端生产构建验证通过

### 阶段 8.3：前端页面 MVP - 网络页（任务 19）
完成时间：2026-03-21

已完成：
- 网络页时间区间选择补齐
- 增加最近 1 小时 / 6 小时 / 24 小时快捷查询
- 按 app 上传/下载汇总表格补齐
- 展示总上传 / 总下载 / 参与统计 app 数
- WAN / LAN 占比展示补齐（简化条形占比）
- Top N 排行展示补齐
- 网络页前端构建验证通过

### 阶段 8.4：前端页面 MVP - 设置页（任务 20）
完成时间：2026-03-21

已完成：
- 设置页表单体验补强
- 展示 HTTP 端口
- 展示硬件采样间隔
- 展示网络刷新间隔
- 展示默认时间粒度
- 展示历史保留天数
- 展示默认 Top N
- 增加前端范围校验提示
- 增加重新加载 / 撤销修改 / 保存反馈
- 明确提示端口修改将在下次启动生效
- 设置页前端构建验证通过

## 当前进行中
- 阶段 8：前端页面 MVP 已完成，下一步进入测试与验证阶段（任务 21 起）

## 备注
- 当前总览页、网络页、设置页都已具备 MVP 基础展示与交互；后续重点进入联调验证与稳定性检查。
- 当前硬件采样在本机可获取 CPU 使用率、内存、开机时长；部分温度/频率传感器在本机返回 null，已按部分数据快照处理。
- 当前 ETW 采集器在普通权限下不会进入运行态；提权后已验证可正常启动并捕获事件，因此后续正式运行需要管理员权限或具备相应权限的服务身份。
